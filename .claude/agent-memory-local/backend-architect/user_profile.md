---
name: user-profile
description: User's role and collaboration preferences on the artshop backend project
metadata:
  type: user
---

User is building an Art Supplies e-commerce backend (Spring Boot + MyBatis + PostgreSQL). Works on Windows (PowerShell), git user "Seima Heav".

Expects strict adherence to CLAUDE.md conventions, corporate layer separation (controller -> service -> mapper), RFC 7807 ProblemDetail error handling, and the dependency approval loop (halt + formal request before touching pom.xml). Wants a concise file-by-file summary at the end of tasks with explicit flagging of decisions needed.
